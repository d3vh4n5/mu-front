import * as pj from '../../package.json'

export const config = {
    project: {
        alias: "Mu Campana",
        icon: "",
        name: pj.name,
        version: pj.version
    },
    server: {
        version: "99b + DL"
    }
}